---
description: Stock_Analysis 项目 Agent 执行规则
alwaysApply: true
---

# Stock Analysis Agent Rules

- **读取顺序**：先看 **`AGENTS.md`** 恢复上下文；需要完整条文、公式、例外时再查 **`AI_股票对话提示.md`**；本文件只保留必须常驻的硬规则。
- 仓库内 **Python 包与文件速查**：`docs/NAMESPACE.md`（`analysis` = 业务分析/归一化/指标/台账，`tools` = 外部 API 客户端实现，`intel` = 研报客封装）。
- 目录边界（强制）：`analysis/price_feeds.py` 仅做 provider 分发与 OHLCV 归一化；`tickflow/gateio/goldapi` 外部 API 请求位于 `tools/<provider>/client.py`；`market_data/` 仍是板块/资金流结构化数据预留域。

- **禁止串用数据源（强制）**
  - **仅用研报产物（market-intel）时**：不得从研报文本推断或输出具体 **entry/stop/tp**、手数或「已可下单」类执行结论；不得冒充交易所实时成交或官方资金流。
  - **仅用或主用 K 线产物（crypto-kline / macro-kline）时**：不得把研报标题、摘要或评级表述为「已验证的价格触发」或「机构已确认」式定论；技术触发只引用 OHLCV / `ai_overview.json` / 台账结构字段。
  - **`market_data/` 未接**：不得编造官方板块成分、交易所级逐笔资金流等结构化数据；可改用研报关键词作**线索**并声明非官方成分。
  - 叙事与技术**同一轮并存**时：须分栏标注 **叙事证据（研报检索）** 与 **技术证据（OHLCV / ai_overview）**；二者冲突时不强行拧成单一方向，默认偏观察、降执行强度。完整写法见 `AI_股票对话提示.md` §2.1。

- 始终使用简体中文回复。
- 写作风格：自然、直接、清晰；先结论后依据；避免模板化空话。
- **首次对话 / 从其他 Agent 入口**：若用户尚未说明 **整体资产规模（可区间）** 与 **风险偏好（至少含单笔最大亏损占权益比例或保守/均衡/进取）**，应先完成简短问答，或请用户确认使用「默认演示画像」并在回复中写明假设；**之后**再跑行情 CLI。同一对话内已给出画像则可沿用。完整问法与口径见 `AI_股票对话提示.md` §0.1。
- **执行顺序**：按 `AGENTS.md` §2 执行；即先风险画像，再路由，再跑 CLI，再按固定顺序读产物，再输出解读与复核时间。
- **CLI 与产物路径**：默认命令、研报命令、板块检索命令、输出目录与读取顺序统一以 `AGENTS.md` §3～§4 为准；本文件不重复展开命令示例。
- **个性化仓位与开单**：仅在已建立风险画像且已读 `ai_overview.json` 时输出；entry/stop/tp/triggered 只能来自技术 JSON，不得从研报推导。公式与写法见 `AI_股票对话提示.md` §3.1。
- **新增标的**：先校验 API 可用性，再更新 `config/market_config.json`；字段要求与风格统一以 `AI_股票对话提示.md` §1 为准。
- **输出底线**：先逐标的，再跨标的；每个标的至少包含综合倾向、关键位、触发条件、风险点、免责声明；全文末尾必须有复核时间。具体模板见 `AGENTS.md` §2 与 `AI_股票对话提示.md` §3、§6。
- **台账**：**PostgreSQL** `journal_*` 存结构快照（非交易所成交）；写入前经 `config/analysis_defaults.yaml` 的 `min_journal_rr` 与可选 `journal_quality`，逻辑见 `analysis/journal_policy.py`。加密 / `gateio` 可并存 **`tactical` 与 `swing`** 两条候选（分槽去重）。RR 过滤不保证可执行。
- 合规口径：仅作技术分析与程序化演示，不构成投资建议。
- 数据源默认 `tickflow`；若用户指定可切换 `gateio`（加密）或 **`goldapi`**（贵金属，默认内置 appkey；`--symbol` 用配置里贵金属标的如 `AU9999`）。
