---
description: Stock_Analysis 项目 Agent 执行规则
alwaysApply: true
---

# Stock Analysis Agent Rules

- 始终使用简体中文回复。
- 写作风格：自然、直接、清晰；先结论后依据；避免模板化空话。
- 股票行情相关请求默认先执行：
  - `python3 Stock_Analysis/stock_analysis.py --market-brief --report-only --out-dir Stock_Analysis/output`
- 若用户明确要看**研报线索**、**机构观点**、**行业/板块资金与配置叙事**（不等同于交易所逐笔资金流），在行情命令基础上追加研报客搜索：
  - 多标的默认（用标的名称做关键词）：  
    `python3 Stock_Analysis/stock_analysis.py --market-brief --report-only --out-dir Stock_Analysis/output --with-research --research-n 5`
  - 单标的且用户给了研报关键词（含板块名/主题词）：  
    `python3 Stock_Analysis/stock_analysis.py --symbol <SYMBOL> --report-only --out-dir Stock_Analysis/output --with-research --research-n 5 --research-keyword "<关键词>"`
  - 说明：研报客输出写入 `Stock_Analysis/output/research/<UTC日期>/`；`full_report.md` / `ai_overview.json` 会附带摘要字段；**搜索不要求 API Key**，下载才需要 `YANBAOKE_API_KEY`（见 `Stock_Analysis/tools/yanbaoke/SKILL.md`）。
  - 边界：研报客提供的是**研报检索与摘要线索**，不是保证准确的实时“主力资金净流入”数据；若用户要的是交易所级资金流，应明确说明本项目当前不做该数据源。
- 若用户想看**新的股票**，先更新 `Stock_Analysis/market_config.json`：
  - 在 `assets` 追加 `symbol` / `name` / `market` / `data_symbol`
  - 需要默认纳入简报时，再把 `symbol` 加入 `default_symbols`
- 读取产物顺序固定为：
  1) `ai_brief.md`
  2) `ai_overview.json`
  3) `full_report.md`
- 输出顺序固定为：先逐标的解读，再跨标的总结。
- 每个标的解读至少包含：综合倾向、关键位（含 Fib 区间）、触发条件、风险点、免责声明。
- 合规口径：仅作技术分析与程序化演示，不构成投资建议。
- 数据源默认 `tickflow`；若用户指定可切换 `akshare` 或 `alltick`。
